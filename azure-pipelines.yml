# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: DotNetCoreCLI@2
  inputs:
    command: publish 
    arguments: -r win-x64 -p:PublishSingleFile=True -o $(Build.ArtifactStagingDirectory)
    projects: '**/*.csproj'
    publishWebProjects: false

- task: VSTest@2
  inputs:
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- publish: $(Build.ArtifactStagingDirectory)
  artifact: Application
- task: file-creator@6
  inputs:
    filepath: '$(Build.ArtifactStagingDirectory)\appsettings.json'
    filecontent: |
      {
        "ApiConfig": {
          "Discord": {
            "DiscordToken": "ODYzNDUzOTg1Nzk3MzA4NDU2.YOnIMA.HC9VagerZu0fj7sJcAqhFSqwc70"
          },
          "AzureCognitive": {
            "Region": "eastus",
            "Key": "e8f355d066d7433e869ff10944d97b4c",
            "VoiceFilters": {
              "Region": "en-us",
              "Type": "onlineneural"
            }
          },
          "Weather": {
            "Token": "3256b6ea4b6de1edbf1326f9ae3f69d4"
          }
        }
      }
    fileoverwrite: true